enum EPriorityLevel {
    PriorityNone = 0,
    PriorityLow = 1,
    PriorityMid = 2,
    PriorityHigh = 3,
    PriorityCritical = 4,
    EPriorityLevel_MAX = 5,
};

enum ESpatialDimension {
    XY = 0,
    XYZ = 1,
    ESpatialDimension_MAX = 2,
};

